# Topic Integration

When a Topic contains more than one Team Problem, record cross-Problem interfaces, selected artifacts, compatibility checks and end-to-end acceptance evidence here. Leave this directory empty when no such integration evidence exists.

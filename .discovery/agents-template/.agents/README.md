# Search Route CLI Skill

This directory belongs to concrete search-route workspaces created from the
template.

The template keeps only the `explore-cli` skill here. Builder and Auditor
role instructions live in `goals/route_builder.md` and
`goals/route_auditor.md` so each active `/goal` owns the role contract.
